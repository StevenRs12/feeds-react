import "./Header.scss";

const Header = () => {
  return (
    <header className="header">
      <div className="logo">
        <img src="icon.svg" alt="Logo" />
      </div>
    </header>
  );
};

export default Header;
